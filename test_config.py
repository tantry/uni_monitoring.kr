import config

print("Config variables:")
print(f"BOT_TOKEN exists: {'BOT_TOKEN' in dir(config)}")
print(f"CHAT_ID exists: {'CHAT_ID' in dir(config)}")
print(f"CHANNEL_ID exists: {'CHANNEL_ID' in dir(config)}")
